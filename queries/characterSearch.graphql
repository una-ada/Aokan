query ($search: String) {
  Page {
    characters(search: $search) {
      id
      name {
        full
        native
      }
      image {
        large
        medium
      }
      description(asHtml: true)
    }
  }
}
